after pulling the files run:

run these terminal cmds(linux)

npm install

Install the specific libraries used in the code (Icons, Animations, Tailwind)
npm install lucide-react motion
npm install -D tailwindcss @tailwindcss/vite

to run the build run
npm run dev
